apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ template "otterize.sniffer.name" . }}
spec:
  selector:
    matchLabels:
      app: {{ template "otterize.sniffer.name" . }}
  template:
    metadata:
      labels:
        app: {{ template "otterize.sniffer.name" . }}
    spec:
      containers:
      - name: {{ template "otterize.sniffer.name" . }}
        image: {{ .Values.registry }}:{{ .Values.snifferImage }}
        imagePullPolicy: Always
        resources:
          limits:
            memory: 200Mi
          requests:
            cpu: 100m
            memory: 200Mi
        env:
          - name: OTTERIZE_MAPPER_API_URL
            value: http://{{ template "otterize.mapper.name" . }}:9090/query
          - name: OTTERIZE_DEBUG
            value: {{ .Values.debug | quote }}
        volumeMounts:
          - mountPath: /hostproc
            name: proc
            readOnly: true
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      volumes:
        - hostPath:
            path: /proc
            type: ""
          name: proc