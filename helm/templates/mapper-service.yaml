apiVersion: v1
kind: Service
metadata:
  name: {{ template "otternose.mapper.name" . }}
spec:
  selector:
    app: {{ template "otternose.mapper.name" . }}
  ports:
    - protocol: TCP
      port: 9090
      name: http
      targetPort: 9090